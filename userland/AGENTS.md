# Sikia runtime tailor

Change only files under `src/`. Never run git commands or modify this file, the kernel, or the shell. Keep the requested change minimal and preserve existing behavior outside the request.
